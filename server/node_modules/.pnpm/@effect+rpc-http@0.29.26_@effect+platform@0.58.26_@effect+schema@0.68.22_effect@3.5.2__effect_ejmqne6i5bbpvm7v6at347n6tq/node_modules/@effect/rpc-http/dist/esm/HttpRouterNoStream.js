import * as ServerRequest from "@effect/platform/HttpServerRequest";
import * as ServerResponse from "@effect/platform/HttpServerResponse";
import * as Router from "@effect/rpc/Router";
import * as Effect from "effect/Effect";
/**
 * @since 1.0.0
 * @category conversions
 */
export const toHttpApp = self => {
  const handler = Router.toHandlerEffect(self);
  return ServerRequest.HttpServerRequest.pipe(Effect.flatMap(_ => _.json), Effect.flatMap(handler), Effect.map(ServerResponse.unsafeJson));
};
//# sourceMappingURL=HttpRouterNoStream.js.map